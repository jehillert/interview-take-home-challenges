// cookie key that stores user data between page reloads.
const cookieId = 'userData';

// drop-down default values
const adultArr = [1, 2];
const childArr = [0, 1, 2];

// enumeration of room numbers
const roomIdArr = ['1', '2', '3', '4'];

export { adultArr, childArr, cookieId, roomIdArr };
